@model IEnumerable<SaveOurSpecies.Models.Adoption>

@{
    ViewBag.Title = "Index";
}

<h2>Adoption Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Animal.CommonName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Patron.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Donate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Animal.CommonName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Patron.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Donate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
